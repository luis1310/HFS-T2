# Dependencias básicas
numpy>=1.24.0
matplotlib>=3.7.0
pandas>=2.0.0
seaborn>=0.12.0

# Configuración y utilidades
pyyaml>=6.0
tqdm>=4.65.0
psutil>=5.9.0  # Requerido para paralelización (detección de núcleos y memoria)

# Testing y calidad de código
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-timeout>=2.1.0  # Para tests con límite de tiempo
black>=23.0.0
flake8>=6.0.0
isort>=5.12.0  # Para ordenar imports (CI)

# Opcional: aceleración
numba>=0.57.0
